# 진행 현황 (Progress Tracker) — 2차 개발

> 이 파일은 `docs/2차/WBS.md`의 TASK 목록을 체크리스트로 옮긴 것입니다. **TASK 하나가 완료되면 PM 확인 후 이 파일의 해당 체크박스만 `[ ]` → `[x]`로 바꿉니다.** `WBS.md` 자체(설계 문서)는 수정하지 않습니다 — 계획과 진행상황을 분리해서, 계획 문서가 진행 중에 계속 바뀌어 보이지 않게 합니다.
>
> **업데이트 규칙**: Claude Code는 TASK 완료를 PM이 확인("완료" 또는 "다음" 등 승인 발언)한 직후에만 체크박스를 바꿉니다. 스스로 판단해서 미리 체크하지 않습니다.
>
> `PM-` 코드 TASK는 Claude Code가 아닌 PM이 직접 수행하는 작업입니다(레포 루트 Git 명령, 외부 서비스 가입 등). 완료 여부를 PM이 직접 체크합니다.

---

## 선행작업

- [x] **PM-0-1** (PM 작업) Git 태그 `v1.0.0` 생성 및 push
  > 2026-07-13 완료. `main` HEAD(`0b81d5c`, PR #11 병합 커밋)에 annotated 태그 생성 및 origin push 완료.
- [ ] **S-1-13** (시스템) 소셜 로그인(OAuth Provider) 프로덕션 실사용 검증 확정 — 2차 마지막 TASK로 배치

---

## 3장 — 데이터베이스/Storage 구현

- [x] **S-2-1** (시스템) `UTILITY_BILL_ITEM` 테이블 마이그레이션
  > 2026-07-13 완료. `supabase/migrations/20260713042138_create_utility_bill_item_table.sql` — `데이터구조설계.md` §2-3 정의 그대로(`id`/`user_id`/`name`/`has_usage`/`usage_unit`/`source_labels`/`is_active`/`created_at`), `user_id` 인덱스 추가. RLS는 활성화만 해두고 정책/GRANT는 계획대로 S-2-4에서 3개 테이블 한번에 작성 예정. `supabase db reset`으로 로컬 검증, `packages/types/src/database.generated.ts` 타입 재생성, typecheck 통과.
- [x] **S-2-2** (시스템) `UTILITY_BILL_RECORD` 테이블 마이그레이션(`UNIQUE(user_id,period)`)
  > 2026-07-13 완료. `supabase/migrations/20260713044514_create_utility_bill_record_table.sql` — `UNIQUE(user_id, period)`에 더해 `transaction_id`에도 `UNIQUE` 제약 추가(설계서의 "TRANSACTION 1:1" 관계를 DB 레벨에서 강제), `transaction_id`는 `ON DELETE CASCADE`(트랜잭션 하드 삭제 시 딸린 레코드도 함께 삭제). `db reset` 검증 + 타입 재생성 + typecheck 통과.
- [x] **S-2-3** (시스템) `UTILITY_BILL_ITEM_VALUE` 테이블 마이그레이션(`UNIQUE(record_id,item_id)`)
  > 2026-07-13 완료. `supabase/migrations/20260713044828_create_utility_bill_item_value_table.sql` — `UNIQUE(record_id, item_id)`, `amount`는 `TRANSACTION_DETAIL`과 동일 관례로 `CHECK (amount > 0)`. RLS는 `user_id` 없이 부모 RECORD 기준(TRANSACTION_DETAIL과 동일 원칙), 정책 자체는 S-2-4에서 작성 예정. `db reset` 검증 + 타입 재생성 + typecheck 통과.
- [x] **S-2-4** (시스템) 3개 테이블 RLS 정책 + `authenticated` GRANT 작성
  > 2026-07-13 완료. `supabase/migrations/20260713045103_utility_bill_rls_policies_and_grants.sql` — 데이터정책*및*시드정의서.md 3장 표 그대로 반영: `utility_bill_item`(본인만 SELECT/INSERT/UPDATE, DELETE 정책 없음), `utility_bill_record`(본인만 SELECT/INSERT/UPDATE/DELETE), `utility_bill_item_value`(부모 RECORD 기준, TRANSACTION_DETAIL과 동일 패턴). GRANT는 RLS 정책과 정확히 같은 범위로 같은 마이그레이션에 함께 작성(GRANT 누락 이력 재발 방지). `db reset` 검증 통과.
- [x] **S-2-5** (시스템) Storage 비공개 버킷 생성 + 경로기반 RLS
  > 2026-07-13 완료. `supabase/migrations/20260713045551_create_utility_bills_storage_bucket.sql` — `utility-bills` 비공개 버킷(`public=false`) 생성, `storage.objects`에 경로 첫 세그먼트(`storage.foldername(name)[1]`)가 본인 `user_id`와 일치하는 경우만 SELECT/INSERT/UPDATE/DELETE 허용. 경로 규칙 `{user_id}/{period}.{ext}`(데이터구조설계.md §5). `db reset` 검증 + 로컬 Storage API로 버킷 생성·`public:false` 확인.
- [x] **S-2-6** (시스템) 업로드용 signed URL 발급 로직 + 클라이언트 직접 업로드 연동
  > 2026-07-13 완료. `app/(app)/utility-bills/actions.ts`의 `createUploadUrlAction` — 경로의 `user_id`는 클라이언트 입력이 아니라 서버에서 인증된 세션(`auth.getUser()`)으로만 구성해 타 사용자 경로 요청 자체를 차단(스토리지 RLS와 별개의 방어선). `lib/utility-bill-upload.ts`의 `uploadUtilityBillFile`이 브라우저에서 signed URL로 Storage에 직접 업로드(Vercel 함수 4.5MB 본문 제한 우회). 아직 화면(F-2-1-1)이 없어 로컬 Supabase에 대해 별도 스크립트로 실제 흐름 검증: signed URL 발급→직접 업로드→본인 파일 조회 정상, 타 사용자의 signed URL 발급 시도(RLS 위반)와 파일 다운로드 시도(RLS로 not found) 둘 다 차단 확인. lint/typecheck 통과.

## 4장 — OCR 연동

- [x] **PM-4-1** (PM 작업) OCR 벤더 선정/가입/API 키 발급
- [x] **S-2-7** (시스템) OCR API 연동 PoC(좌표 반환, 처리시간 실측)
  > 2026-07-13 완료. `lib/ocr/`에 `OCRProvider` 인터페이스(`types.ts`) + `GoogleVisionProvider`(`google-vision-provider.ts`, `@google-cloud/vision` SDK 없이 REST `fetch` 직접 호출) + `createOCRProvider()` 팩토리(`index.ts`) — PM 요청대로 향후 `PaddleOCRProvider` 등 엔진 교체/병행 확장 가능한 구조. 실제 Google Vision API로 PoC 검증: 처리시간 589ms, 단어별 텍스트+4점 폴리곤 좌표 정상 반환. 진행 중 발견/해결한 이슈 — ①GCP 프로젝트 결제 미연결(`BILLING_DISABLED`) → PM이 결제 계정 연결 ②API 키의 API 제한(API restrictions) 설정으로 Vision API 자체가 차단(`API_KEY_SERVICE_BLOCKED`) → PM이 Cloud Vision API 허용 목록 추가. **주의**: 이번 검증은 로고 이미지(영문)로 연동 자체만 확인한 것 — 실제 한글 관리비 고지서(표 형태) 정확도는 미검증, S-2-8/9에서 실제 고지서 사진으로 별도 확인 필요.
- [x] **S-2-8** (시스템) 업로드 파일 → OCR 추출 → 라벨/금액 파싱 API
  > 2026-07-13 완료. `lib/utility-bill-parse.ts`의 `parseUtilityBillCandidates` — 단어 단위 bounding box를 y좌표 근접도로 줄 단위로 묶고(x좌표로 좌→우 정렬), 각 줄에서 금액 토큰(쉼표 포함 숫자+선택적 "원", 3자리 미만은 오탐 방지로 제외)을 오른쪽부터 찾아 라벨/금액으로 분리. `app/(app)/utility-bills/actions.ts`에 `extractUtilityBillAction` 추가 — 업로드된 파일을 Storage에서 다운로드(경로가 본인 것인지 재확인) 후 `OCRProvider`로 텍스트 추출 → 파싱까지 연결. 단위테스트 8건(`__tests__/utility-bill-parse.test.ts`) 통과. **실제 검증**: Playwright로 한글 관리비 고지서 형태 합성 이미지(일반관리비/전기료/수도료/급탕요금/합계 5개 행) 생성 → 실제 Vision API로 OCR→파싱 전체 파이프라인 실행 → 5개 항목 전부 정확한 라벨+금액 추출 확인(Vision이 "원"을 단어마다 붙이거나 분리하거나 생략하는 케이스 전부 정상 처리). **발견한 이슈**: "일반관리비"가 "일반"+"관리비" 두 단어로 인식되어 합칠 때 공백이 들어감("일반 관리비") — `source_labels` 정확 매칭 시 공백 차이로 어긋날 수 있어 S-2-9에서 공백 제거 후 비교 필요. lint/typecheck/test 통과.
  > **2026-07-13 재작업**: 실제 폰카메라 사진(`docs/sample/`, `.gitignore` 처리)으로 재검증한 결과, 위 단순 y좌표 그룹핑은 관리비 고지서처럼 여러 구역이 나란히 배치된 다열 문서에서 라벨/금액이 다른 구역과 섞이는 문제가 확인되어(오탐 다수) 전면 재작업. `extractUtilityBill()`(`utility-bill-parse.ts`)로 교체 — "지침"/전기·온수·수도·난방(사용량별 지침표), "부가세제외항목"(관리비 상세표), "납기내금액"+년/월 정규식(총액/청구월) 등 **특정 문구를 앵커로 좌표 범위를 좁혀(가상 크롭) 검색**하는 방식으로 변경, 실제 사진에서 사용량표 4/4·상세표 6/6·총액/청구월 전부 정확 추출 확인. `extractUtilityBillAction` 반환 타입을 `{period, total, usageTable, items}` 구조로 변경. 단위테스트 6건 재작성(실측 비율 재현 픽스처), typecheck/lint/전체 테스트(101건) 통과. **범위 결정(PM, 2026-07-13)**: 이 앵커들은 현재 샘플(목동현대파리지앙) 양식 고정값이며, "최초 업로드 크롭 UI + 사용자별 학습 라벨 앵커" 일반화 설계는 `docs/백로그.md` B-13으로 이관 — 이번 TASK는 **재업로드(현재 알려진 양식) 케이스만** 지원.
- [x] **S-2-9** (시스템) 형식변경 판별 로직(매칭률 50% 기준) — 축소판
  > 2026-07-13 완료(축소판, PM 결정). 화면 분기(케이스 A/B, 항목 선정 화면 재진입 등 F-2-1-2 몫)를 뺀 **매칭률 계산 순수 함수만** 구현: `lib/utility-bill-format-match.ts`의 `calculateFormatMatchRate`(추출 라벨 vs 활성 항목 `source_labels` 비교, 분모=기존 지정 항목 수, 별칭 중 하나라도 일치하면 매칭) + `isSameFormat`(50% 기준 판정). S-2-8에서 발견한 "공백 차이로 인한 오탐"(OCR이 "일반관리비"를 "일반"+"관리비"로 쪼개 합칠 때 공백 발생) 방지를 위해 비교 전 공백 제거 정규화 반영. 단위테스트 9건(`__tests__/utility-bill-format-match.test.ts`) 통과, 전체 테스트 110건/typecheck/lint 통과. 동적 형식변경 감지(실제 업로드 플로우 연동)와 최초 업로드 크롭 UI는 `docs/백로그.md` B-13으로 범위 이관.

## 5-1장 — 명세서 업로드 화면 (F-1-2-1)

- [x] **F-2-1-1** (앱기능) 업로드 화면 기본 골격(드래그앤드롭/카메라)
  > 2026-07-13 완료. `/utility-bills/upload`(서버 컴포넌트 `page.tsx` + 클라이언트 `UtilityBillUploadSection.tsx`) — 드래그앤드롭 영역 + "사진 촬영/파일 선택" 버튼(모바일은 `capture="environment"`로 카메라 우선) + 최근 3개월 등록 현황 목록(`UTILITY_BILL_RECORD.period` 조회). "통계 보기" 버튼은 대상 화면(F-2-3-x)이 아직 없어 죽은 링크 방지 원칙에 따라 이번엔 제외, 그 화면 완료 시 추가 예정. 실제 업로드 트리거(signed URL 호출)와 재업로드 충돌 판정은 F-2-1-2 범위 — 이번엔 파일 선택/미리보기 골격까지만. 모바일(390px)/데스크탑(1280px) 실제 브라우저 스크린샷으로 레이아웃 확인, 회원가입 테스트 계정으로 실제 파일 선택 인터랙션(파일명 표시)까지 프로덕션 빌드(`next build && next start`)로 검증 완료 — dev 서버(Turbopack)는 이 샌드박스의 WebSocket 제약으로 HMR 하이드레이션이 막혀 클릭 반응이 안 되는 환경 특유 현상 확인(코드 문제 아님, B-4와 유사 계열). typecheck/lint 통과.
- [x] **F-2-1-2** (앱기능) 재업로드 충돌 판정 + 케이스 A/B 팝업
  > 2026-07-13 완료. **설계 순서 변경(PM 확인)**: 화면설계 §1-2 원안("업로드→충돌확인→OCR")은 청구월(period)이 OCR로만 확정되는데 Storage 경로 자체가 `{user_id}/{period}.{ext}`라 순환 문제가 있어, "OCR 먼저(파일을 Storage에 안 올리고 바로 서버 액션에 전송) → 확정된 period로 충돌 확인 → (F-2-1-3에서) 그제서야 Storage 저장" 순서로 변경. `extractUtilityBillAction`을 Storage 경로 대신 `FormData`로 파일을 직접 받도록 변경, `checkPeriodConflictAction` 신규 추가(케이스 A/B 판정). 팝업은 기존 공용 컴포넌트 `SuccessDialog`(케이스 A, 확인만)/`ConfirmDialog`(케이스 B, 재업로드 확정) 재사용.
  >
  > **실사진 검증 중 발견한 버그 2건, 둘 다 수정**: ①`actions.ts`(`"use server"`)에서 상수(`UTILITY_BILLS_BUCKET`)를 `export`하고 있었는데, 클라이언트 컴포넌트가 이 파일을 처음 import하면서 Next.js가 "모든 export는 서버 액션(비동기 함수)이어야 함" 규칙에 걸려 빌드 자체가 깨짐 확인 → export 제거(파일 내부 전용으로 전환). ②실제 폰카메라 사진(2~3MB)을 Server Action으로 직접 보내니 기본 본문 크기 제한(1MB)에 걸려 500 에러 발생 확인 → `next.config.ts`에 `serverActions.bodySizeLimit: "4mb"` 상향 + `lib/image-compress.ts`(Canvas 기반 리사이즈/압축, 데이터구조설계 §5 "저장 전 압축" 원칙 구현) 신규 추가해 이중 방어.
  >
  > **또 다른 사고 발견/수정(중요)**: 위 버그를 실제 브라우저로 검증하려고 `next build && next start`(프로덕션 빌드)를 여러 번 돌렸는데, 이 프로젝트의 `.env.local`이 로컬이 아니라 **실제 운영 클라우드 Supabase 값**이고 `next dev` 전용 오버라이드(`.env.development.local`)는 `next start`엔 적용 안 된다는 걸 뒤늦게 확인 — 그 사이 클라우드 Auth에 더미 테스트 계정이 몇 개 생성됨(PM이 직접 확인 후 삭제 조치, cascade로 관련 category/payment_method 행도 함께 삭제됨). 재발 방지로 `.env.production.local`을 새로 만들어 `next start`도 로컬 Supabase로 강제 격리, `.env.local`엔 경고 주석 추가. 이후 로컬로 재검증.
  >
  > **최종 검증(로컬, 실제 사진)**: 회원가입 테스트 계정 + 실제 관리비 고지서 사진으로 "충돌 없음" 경로 정상 확인("2026-01 청구월 · 총액 165,600원" 표시). 케이스 A/B(팝업 분기)는 코드는 스펙대로 구현하고 기존 검증된 Dialog 컴포넌트를 그대로 재사용했지만, 로컬 DB의 `service_role`이 `payment_method`/`category` 테이블에 대한 GRANT 자체가 없어(1차부터 있던 기존 갭 — `synonym_dictionary` 외 테이블엔 service_role GRANT가 애초에 없었음, 오늘 처음 service_role로 직접 시드하려다 발견) 자동화된 시드 데이터로 E2E 재현은 못 했음. 또한 케이스 A(수동 등록 존재)는 애초에 "수동 지출 입력 시 UTILITY_BILL_RECORD(source=MANUAL) 생성" 로직이 아직 어디에도 구현되어 있지 않아(F-2-4-3은 "변경 없음"으로 명시) 지금 시점엔 실제로 발생할 수 없는 케이스이기도 함 — 실사용 가능한 시점(F-2-1-3 완료 후 케이스 B, F-2-4-3 완료 후 케이스 A)에 재검증 권장. typecheck/lint 통과.
- [x] **F-2-1-3** (앱기능) 업로드 완료 확인 화면(매칭값+총액+저장)
  > 2026-07-13 완료. 확인 화면(총액/청구월 + 항목별 금액 리스트 + 합계 불일치 시 안내 배너 + "저장" 버튼)을 `UtilityBillUploadSection.tsx`에 이어 붙이고, 저장 로직(`saveUtilityBillAction`)을 `actions.ts`에 추가.
  >
  > **결제수단 확정(PM 결정, 설계서에 없던 규칙)**: 직전에 등록한 관리비 명세서가 있으면 "직전 결제수단으로 등록할까요?" 확인 팝업(`ConfirmDialog` 재사용), 없으면 결제수단+지출일을 직접 고르는 팝업(`PaymentMethodPickerDialog` 신규, 지출일 기본값=청구월 말일). `getRecentPaymentMethodAction`/`getActivePaymentMethodsAction` 신규.
  >
  > **저장 로직**: 카테고리="관리비/공과금", 지출처="관리사무소"(없으면 생성), `occurred_at`=확정된 날짜, `raw_payload`=OCR 추출 전체 JSON으로 `TRANSACTION` 생성 → `UTILITY_BILL_RECORD`(source='UPLOAD') 생성 → 추출된 항목(관리비 상세표 + 사용량표 중 금액 있는 행)마다 `UTILITY_BILL_ITEM`을 사용자별 이름 기준으로 upsert 후 `UTILITY_BILL_ITEM_VALUE` 생성 → 마지막에 원본 파일을 Storage에 업로드(S-2-6 signed URL 방식, F-2-1-2에서 미뤄뒀던 단계). 항목 선정 화면(F-2-2-1, 백로그 B-13)이 없어 추출된 항목을 사람이 고르는 절차 없이 전부 자동 등록 — "재업로드 기준" 진행 방침에 따른 임시 처리로 명시.
  >
  > **재업로드(케이스 B) 시**: 저장 전 기존 `TRANSACTION`을 삭제해 `UTILITY_BILL_RECORD`/`UTILITY_BILL_ITEM_VALUE`가 연쇄 삭제되게 하고, 같은 Storage 경로의 기존 파일도 먼저 제거(`remove`) 후 재업로드.
  >
  > **체크포인트 검증 완료(F-2-1-2에서 남겨둔 것)**: 실제 사진으로 최초 저장(결제수단 직접 선택 → 저장 → `/expenses`에 "관리비/공과금·관리사무소 165,600원 2026.01.31." 정상 표시)과 **케이스 B 재업로드까지 전부 실제 브라우저로 검증** — 재업로드 시 "직전 결제수단으로 등록할까요?" 팝업이 정확히 뜨는 것도 확인, 재업로드 후 지출 내역에 중복 없이 1건만 남는 것(기존 트랜잭션 삭제→연쇄 삭제 정상 동작)까지 확인. 케이스 A는 F-2-4-3 완료 후 별도 체크포인트대로 검증 예정.
  >
  > typecheck/lint 통과, 프로덕션 빌드(`next build && next start`, 로컬 Supabase로 안전하게 격리된 상태) 기준 실제 검증.
- [x] **T-2-1** (테스트) 업로드 플로우 단위테스트(최초/재업로드/형식변경)
  > 2026-07-18 완료. `__tests__/utility-bill-actions.test.ts` — `createSupabaseServerClient()` 모킹으로 `checkPeriodConflictAction`(none/blocked/confirm_needed) + `saveUtilityBillAction` 단위테스트(이 레포 첫 server action 모킹 테스트 사례). **범위 확정(PM 확인)**: S-2-9 매칭률 판정 함수가 아직 실제 플로우에 연동 안 돼 있어(백로그 B-13), "형식변경"은 UTILITY_BILL_ITEM 라벨 upsert(신규 생성 vs 재사용) 분기로 검증. 부수적으로 `vitest.config.ts`에 `@/*` alias resolve 추가(actions.ts를 처음 직접 import하며 드러난 갭). 9/9 통과, 전체 스위트 131/131·typecheck/lint 통과. `docs/test-scenarios.md`/`scripts/test-data/T-2-1.mjs` 갱신 완료.

## 5-2장 — 항목 선정 화면 (F-1-2-2)

- [ ] **F-2-2-1** (앱기능) 항목 선정 화면(체크박스, 사용량 배지)
  > 2026-07-18 구현 완료, PM 확인 대기. **범위 확정(PM 확인 3건)**: ①추출 결과는 서버 액션이 페이지 이동 간 상태를 못 지켜줘 `lib/utility-bill-pending-storage.ts`(sessionStorage, File은 base64 data URL로 직렬화)로 명시적 전달 ②항목 선정 화면의 "선택한 항목으로 저장"은 선택된 항목만 남기고 기존 확인 화면(F-2-1-3)으로 돌아가는 것까지만 처리 — 새 라벨은 그 화면의 기존 upsert 저장 로직이 그대로 `UTILITY_BILL_ITEM`을 만들어줌 ③이번 TASK 범위에 화면(체크박스/배지/"이번 달에 없음" 구획)뿐 아니라 매칭률 분기 로직(화면설계 §1-2 3번, S-2-9)까지 포함.
  >
  > `checkUtilityBillFormatMatchAction`(actions.ts) 신규 — 사용자의 활성 `UTILITY_BILL_ITEM.source_labels`와 추출 라벨을 비교(`calculateFormatMatchRate`/`isSameFormat`)해, 매칭률 ≥50%면 바로 결제수단 확정(F-2-1-3)으로, 미만(최초 업로드 포함)이면 `/utility-bills/upload/items`(별도 페이지)로 라우팅. 새 페이지는 체크박스 리스트(사용량표 연결 시 "사용량 연결됨" 배지, `hasUsageForLabel` 신규) + 기존 활성 항목 중 이번 추출에 없는 것을 보여주는 "이번 달에 없음" 구획으로 구성. `normalizeLabel`을 `utility-bill-format-match.ts`에서 export해 이 비교에 재사용.
  >
  > **범위 이월(F-2-2-2)**: "이번 달에 없어 해제된 기존 항목을 `is_active=false`로 전환"과 "최소 1개 선택" 검증(화면설계 §2-3)은 아직 구현하지 않음.
  >
  > **실제 브라우저 검증(Playwright 스크립트, 로컬 Supabase, `next build && next start`)**: 신규 가입 계정(활성 항목 0개 → 매칭률 0%)으로 `docs/sample/` 실사진 업로드 → `/utility-bills/upload/items`로 정상 리다이렉트(체크박스 18~19개, "사용량 연결됨" 배지 노출 확인) → 항목 1개 해제 후 "선택한 항목으로 저장" → `/utility-bills/upload`로 복귀해 파일 선택 화면을 다시 보여주지 않고 바로 결제수단 선택 팝업(최초라 "직전 결제수단" 없음) → 확인 → 총액/청구월 확인 화면에 해제한 항목이 정확히 빠진 채 표시 → 저장 → `/expenses`에 정상 반영. 콘솔 에러 없음. typecheck/lint/test(131/131) 통과.
- [ ] **F-2-2-2** (앱기능) 최소 1개 선택 검증 + 저장
- [ ] **T-2-2** (테스트) 항목 선정 E2E

## 5-3장 — 명세서 통계 화면 (F-1-2-3)

- [ ] **F-2-3-1** (앱기능) 총액 추이 차트
- [ ] **F-2-3-2** (앱기능) 항목별 추이 차트(null "-" 처리)
- [ ] **F-2-3-3** (앱기능) 전월대비 증감률 카드 + 항목별 비중 도넛
- [ ] **T-2-3** (테스트) 통계 화면 단위테스트

## 5-4장 — 총액 불일치 알림 + 기존 화면 연계 (F-1-2-4, F-1-2-5)

- [ ] **F-2-4-1** (앱기능) 지출 수정 화면 안내 배너 추가
- [ ] **F-2-4-2** (앱기능) 통계 화면 알림 아이콘 + 팝업
- [ ] **F-2-4-3** (앱기능) 지출 입력 화면 회귀 확인
  > **범위 확정(2026-07-13 PM 결정, 착수 전 메모)**: WBS 원문("변경 없음")보다 범위가 넓어짐 — 화면 자체는 그대로 두되, 카테고리="관리비/공과금"으로 저장 시 서버 로직에서 `UTILITY_BILL_RECORD`(`source='MANUAL'`, `period`=`occurred_at` 연-월)를 함께 생성하는 훅을 추가해야 함. F-2-1-2 검증 중 "이 훅이 없으면 케이스 A(수동 등록 존재 시 업로드 차단)가 영원히 발동 안 함"이 확인되어 결정. 상세는 `화면설계.md` §4-1, `데이터구조설계.md` §3-2 참고.
  > **체크포인트**: 이 훅을 추가하는 즉시 케이스 A가 처음 발동 가능해지므로, **이 TASK 완료 시 F-2-1-2에서 자동화 못 했던 케이스 A(차단 팝업) E2E도 같이 검증**할 것 — 관리비/공과금으로 수동 지출 등록 → 같은 달 명세서 업로드 시도 → 차단 팝업 뜨는지 확인. 잊지 말고 진행.
- [ ] **T-2-4** (테스트) 관리비 전체 E2E

## 5-5장 — 메뉴/문서 반영

- [ ] **F-2-5-1** (앱기능) "관리비 명세서" 사이드바 메뉴 추가(`nav-items.ts`)
- [ ] **S-2-10** (시스템) `기능명세서_IA.md` 1-1장 갱신
